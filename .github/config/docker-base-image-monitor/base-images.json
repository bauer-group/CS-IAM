{
  "$schema": "https://raw.githubusercontent.com/bauer-group/automation-templates/main/.github/config/docker-base-image-monitor/docker-base-images.schema.json",
  "_description": "CS-IAM — upstream images consumed by the stack. The monitor tracks digests and triggers a release when an upstream image moves.",
  "images": [
    {
      "name": "zitadel",
      "image": "ghcr.io/zitadel/zitadel",
      "tag": "latest",
      "variable": "ZITADEL_LATEST_DIGEST",
      "description": "Zitadel OIDC/IAM core image — floating latest release (PostgreSQL 18 needs >= v4.11.0); src/zitadel wrapper rebuilds when the digest moves"
    },
    {
      "name": "ep-zitadel-login",
      "image": "ghcr.io/bauer-group/ep-zitadel/zitadel-login",
      "tag": "latest",
      "variable": "EP_ZITADEL_LOGIN_LATEST_DIGEST",
      "description": "Base for the src/login branding overlay (EP-Zitadel fork) — floating latest; overlay rebuilds when EP publishes a new login image"
    },
    {
      "name": "postgres",
      "image": "postgres",
      "tag": "18-alpine",
      "variable": "POSTGRES_ALPINE_DIGEST",
      "description": "PostgreSQL 18 — Zitadel datastore"
    },
    {
      "name": "python-alpine",
      "image": "python",
      "tag": "3.14-alpine",
      "variable": "PYTHON_ALPINE_DIGEST",
      "description": "Base image for directory-sync and database-backup"
    },
    {
      "name": "alpine",
      "image": "alpine",
      "tag": "3.23",
      "variable": "ALPINE_DIGEST",
      "description": "Base for the provisioner (tofu binary staged in) + machine-key prep step (OpenTofu version pinned via the provisioner Dockerfile ARG)"
    }
  ],
  "settings": {
    "commit-prefix": "chore(deps)",
    "auto-create-variables": true
  }
}
