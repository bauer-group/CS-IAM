# Runtime dependencies for the directory-sync container.
# Floors track the current release so Dependabot stays quiet until a real bump.
pydantic>=2.13,<3
pydantic-settings>=2.14,<3
httpx>=0.28,<1
azure-identity>=1.25,<2
PyJWT>=2.13,<3
cryptography>=44
apscheduler>=3.11,<4
rich>=15.0.0,<16
